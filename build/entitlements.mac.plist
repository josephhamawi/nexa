<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <!--
    Apple Events is the load-bearing one. Hardened runtime is required for
    notarization, and it blocks Apple Events unless this is granted - which
    would silently break every calendar, notes and mail tool in the app while
    leaving the rest working.
  -->
  <key>com.apple.security.automation.apple-events</key>
  <true/>

  <!-- Electron bundles V8, which JITs. Without these the app will not launch
       under hardened runtime. -->
  <key>com.apple.security.cs.allow-jit</key>
  <true/>
  <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
  <true/>

  <!-- Playwright launches its own Chromium from the unpacked asar. -->
  <key>com.apple.security.cs.disable-library-validation</key>
  <true/>

  <key>com.apple.security.network.client</key>
  <true/>
  <key>com.apple.security.files.user-selected.read-write</key>
  <true/>
</dict>
</plist>
